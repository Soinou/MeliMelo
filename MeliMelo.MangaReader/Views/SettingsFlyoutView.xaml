<UserControl
    x:Class="MeliMelo.Views.SettingsFlyoutView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="300"
    Width="500"
>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="35" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Button x:Name="SetDirectory" Content="Set Directory" Margin="2.5" Padding="0" VerticalContentAlignment="Center" />
        <ScrollViewer Grid.Row="1">
            <ListView x:Name="Mangas" BorderThickness="0" Background="Transparent" DisplayMemberPath="Name" Margin="2.5" />
        </ScrollViewer>
    </Grid>
</UserControl>
