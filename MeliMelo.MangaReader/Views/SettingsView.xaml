<UserControl
    x:Class="MeliMelo.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    d:DesignHeight="600"
    d:DesignWidth="1000"
>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35" />
            <RowDefinition Height="35" />
            <RowDefinition Height="35" />
            <RowDefinition Height="35" />
        </Grid.RowDefinitions>

        <ContentControl x:Name="Directory" Width="500" />
        <ContentControl x:Name="ItemHeight" Width="500" Grid.Row="1" />
        <ContentControl x:Name="ScrollSpeed" Width="500" Grid.Row="2" />
        <Button x:Name="Save" Content="Save" Width="200" Grid.Row="3" />
    </Grid>
</UserControl>
