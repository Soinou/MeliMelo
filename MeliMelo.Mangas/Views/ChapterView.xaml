<UserControl
    x:Class="MeliMelo.Views.ChapterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MeliMelo.Views"
    mc:Ignorable="d"
    d:DesignHeight="35"
    d:DesignWidth="400"
    Height="35"
>
    <Grid Margin="0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="70" />
            <ColumnDefinition Width="100" />
        </Grid.ColumnDefinitions>

        <Label x:Name="Title" Content="Title" Style="{StaticResource MeliMelo.Label}" />
        <Label x:Name="Read" Content="Not Read" Style="{StaticResource MeliMelo.Label}" Grid.Column="1" />
        <Button x:Name="Open" Content="Open" Style="{StaticResource MeliMelo.Button}" Grid.Column="2" />
    </Grid>
</UserControl>
