<Controls:MetroWindow
    x:Class="MeliMelo.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
    xmlns:local="clr-namespace:MeliMelo.Views"
    xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
    xmlns:cal="http://www.caliburnproject.org"
    mc:Ignorable="d"
    WindowStartupLocation="CenterScreen"
    BorderThickness="0"
    GlowBrush="Black"
    Height="350"
    Width="600"
>
    <Controls:MetroWindow.RightWindowCommands>
        <Controls:WindowCommands>
            <Button Content="tasks" cal:Message.Attach="ShowTasks" />
            <Button Content="settings" cal:Message.Attach="ShowConfiguration" />
            <Button Content="check for application update" cal:Message.Attach="ApplicationUpdate" />
        </Controls:WindowCommands>
    </Controls:MetroWindow.RightWindowCommands>
    <Grid Margin="10">
        <Controls:TransitioningContentControl Transition="{Binding Transition}" x:Name="ActiveItem" />
    </Grid>
</Controls:MetroWindow>
