<Controls:MetroWindow
    x:Class="MeliMelo.Views.ApplicationUpdateView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
    xmlns:local="clr-namespace:MeliMelo.Views"
    xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
    xmlns:cal="http://www.caliburnproject.org"
    mc:Ignorable="d"
    BorderThickness="0"
    GlowBrush="Black"
    Title="MeliMelo - Application Update"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen"
    Height="130"
    Width="360"
>
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="35" />
            <RowDefinition Height="35" />
        </Grid.RowDefinitions>

        <Label x:Name="VersionMessage" Content="Please wait..." Grid.ColumnSpan="2" />
        <Button x:Name="Cancel" Content="Cancel" Grid.Row="1" />
        <Button x:Name="Update" Content="Update and Restart" Grid.Row="1" Grid.Column="1" />
    </Grid>
</Controls:MetroWindow>
