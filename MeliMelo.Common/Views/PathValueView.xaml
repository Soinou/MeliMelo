<UserControl
    x:Class="MeliMelo.Views.PathValueView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MeliMelo.Views"
    mc:Ignorable="d"
    d:DesignHeight="35"
    d:DesignWidth="500"
>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="100" />
        </Grid.ColumnDefinitions>

        <Label Content="{Binding Name}" Style="{StaticResource MeliMelo.Label}" />
        <Button x:Name="Browse" Content="Browse" Style="{StaticResource MeliMelo.Button}" Grid.Column="1" />
    </Grid>
</UserControl>
